/*

*/
#include <iostream>

using namespace std;

char mostrarApellido(int legajo){
        Empleados aux;
        int pos=aux.buscarLegajoExistente(legajo);
        aux.leerDeDisco(pos);
        aux.getApellido();
        return *aux.getApellido();
    }
    ///Esta una B

///Dos formas por si era que estaba mal, pero ambas me tiran basura
    char mostrarApellido(int legajo){
        Empleados aux;
        int pos=aux.buscarLegajoExistente(legajo);
        aux.leerDeDisco(pos);
        aux.getApellido();
    }
    ///Esta me tira un dividir

    mostrarApellido(reg.getBloqueUno()[i])
int main(){


   return 0;


}
